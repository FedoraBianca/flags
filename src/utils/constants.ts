export const GAME_STATES = {
  NOT_STARTED: 'NOT_DTARTED',
  IN_PROGRESS: 'IN_PROGRESS',
  OVER: 'OVER'
};

export enum PlayerTypes {
  PLAYER_X = 1,
  PLAYER_O = 2
}

export enum WinnerTypes {
  PLAYER_X = 1,
  PLAYER_O = 2,
  DRAW = 0
}